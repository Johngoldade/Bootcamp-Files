// TODO: What will the following line of code log?
// a window that contains a DOM document
console.log(window);

// TODO: What will the following line of code log?
// refers to the document that the window contains
console.log(window.document);

// TODO: What will the following line of code log?
// outputs the content of the HTML
console.log(document.documentElement);

// TODO: What will the following line of code log?
// returns the content of the head element of the HTML
console.log(document.head);

console.log(window.length())